# Docker compose build-time configuration for the FE bundle.
# This URL is what the *browser* uses to reach the API — set it to the
# public hostname of the API in each environment.

VITE_API_BASE_URL=http://localhost:3000/api
