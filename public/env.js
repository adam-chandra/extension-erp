{
  /* placeholder so the bundle build still finds a file at /env.js during
     local `npm run dev`. The real file is generated by docker-entrypoint.sh
     at container start. */
}
window.__ENV__ = window.__ENV__ || {};
